structs:
	A  = combine ( C , { H, V }  );

domain:
	P = { null };
	C = { null };
	Ep = { null };
	Ec = { null };
	A  = { 1 ... n * m  };

constraints:
	fill(A);
	All(a) <- B(A), is_rectangle(a) && solution(a) == |a|;