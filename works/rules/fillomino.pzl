structs:
	A = combine ( C , { H, V } );

domain:
	P = { null };
	C = { 1 ... n * m };
	Ep = { null };
	Ec = { null };
	A = { 1 ... n * m };

constraints:
	fill(A);
	All(a) <- B(A), All(c) <- a, solution(a) == solution(c) && solution(a) == |a|;