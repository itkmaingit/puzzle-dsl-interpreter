structs:
	A = combine ( C , { H, V } );
	Ah = combine ( C , { H } );
	Av = combine ( C , { V } );

domain-hidden:
	P <-> { null } -> { null };
	C <-> { 1 ... 9 } -> { 1 ... 9, undecided };
	Ep <-> { null } -> { null };
	Ec <-> { null } -> { null };
	A <-> { null } -> { null };
	Ah <-> { null } -> { null };
	Av <-> { null } -> { null };

constraints:
	All(a) <- B(Q), (is_rectangle(a));
